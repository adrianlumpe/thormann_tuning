{% extends 'partials/base.html.twig' %}

{% block content %}

  {% for module in page.collection() if module.header.visible %}
      <section id="{{ module.header.id }}">
        {{ module.content }}
      </section>
  {% endfor %}

{% endblock %}